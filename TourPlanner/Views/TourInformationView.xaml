<UserControl x:Class="TourPlanner.Views.TourInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0" Padding="4">
            <TabItem Header="Tour Information" FontSize="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Margin="5 10 5 10" Text="{Binding CurrentTour.Name}" FontSize="28" FontWeight="Bold" TextAlignment="Center" Grid.Row="0"/>
                    <TextBlock Margin="5 10 5 10" Text="{Binding CurrentTour.Description}" FontSize="16" TextAlignment="Justify" Grid.Row="1" TextWrapping="WrapWithOverflow" />

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5 10 5 10">
                        <Label >From: </Label>
                        <TextBlock Text="{Binding CurrentTour.From}" FontSize="16" TextAlignment="Justify" Grid.Row="1" TextWrapping="WrapWithOverflow" />
                    </StackPanel>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="5 10 5 10">
                        <Label >To:</Label>
                        <TextBlock Text="{Binding CurrentTour.To}" FontSize="16" TextAlignment="Justify" Grid.Row="1" TextWrapping="WrapWithOverflow" />
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Tour Image" FontSize="16">
                <TextBlock> Some Random Image </TextBlock>
            </TabItem>
            <TabItem Header="Tour Logs" FontSize="16" Padding="4">
                <local:TourLogListView />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
