<UserControl x:Class="TourPlanner.Views.BackToOverviewButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Grid.Row="0" Margin="20" Padding="5" FontWeight="Bold" Command="{Binding ToOverViewCommand}" Content="{Binding ToMenuLabel}" BorderBrush="{x:Null}" FontFamily="Courier New" FontSize="14" Cursor="Hand" >
            <Button.Background>
                <LinearGradientBrush EndPoint="0,0.5" StartPoint="1,0.5">
                    <GradientStop Color="#FFFF9696" Offset="1"/>
                    <GradientStop Color="#FFFF6F6F" Offset="0"/>
                </LinearGradientBrush>
            </Button.Background>
        </Button>
    </Grid>
</UserControl>
