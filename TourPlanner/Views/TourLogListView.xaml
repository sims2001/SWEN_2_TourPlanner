<UserControl x:Class="TourPlanner.Views.TourLogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListView ScrollViewer.VerticalScrollBarVisibility="Auto" BorderThickness="0" Grid.Column="0" ItemsSource="{Binding AllTourLogs}" >
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridViewColumn Header="Date" Width="auto" />
                    <GridViewColumn Header="Comment" Width="auto"/>
                    <GridViewColumn Header="Difficulty" Width="auto"/>
                    <GridViewColumn Header="Total Time" Width="auto" />
                    <GridViewColumn Header="Rating" Width="auto"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
